This folder is intended to store visual assets used by the README and site.

Place the screenshot image provided by the maintainer here and name it:

  screenshot.png

Once the binary image file is added as `screenshot.png`, it will appear in the repository README.
